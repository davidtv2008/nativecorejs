/**
 * API Endpoint Constants
 *
 * Add your own endpoint groups here as the app grows.
 */

export const API_ENDPOINTS = {
    // Example:
    // USERS: {
    //     LIST: '/users',
    //     DETAIL: (id: string | number) => `/users/${id}`,
    // },
} as const;

export default API_ENDPOINTS;
